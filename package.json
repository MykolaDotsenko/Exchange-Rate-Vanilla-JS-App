{
  "name": "nordrate-currency-converter",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free Nordic currency converter using daily reference rates.",
  "scripts": {
    "test": "node --test",
    "check": "node --check script.js && node --check src/exchange.js && node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
